export interface AISGap { gap_start: string; gap_end: string; gap_duration_minutes: number; }
export interface EvidenceBreakdown { distance_component?: number; time_component?: number; trajectory_component?: number; presence_component?: number; anomaly_component?: number; speed_component?: number; vessel_type_component?: number; [key: string]: number | undefined; }
export interface CandidateVessel { id: string; name: string; mmsi?: string; score: number; risk_level?: string; confidence?: number; distance_km?: number; time_difference_minutes?: number; trajectory_alignment?: number; movement_direction?: string; vessel_type?: string; speed_knots?: number; ais_anomalies?: AISGap[]; score_breakdown?: EvidenceBreakdown; reasons: string[]; }
export interface InvestigationResult { spill: { confidence: number; area_km2: number }; origin: { latitude: number; longitude: number; uncertainty_km: number }; vessels: CandidateVessel[]; geometry?: Record<string, unknown>; prediction?: Record<string, unknown>; oil_quantity?: Record<string, unknown>; }
export interface ForensicReport extends InvestigationResult { incident_id: string; detection_timestamp: string; generated_timestamp: string; investigation_status: string; }